#include<stdio.h>
#include<math.h>

int main(void) {
    double x, z, y;
    printf("x="); scanf("%lf", &x);
    printf("z="); scanf("%lf", &z);

    y = tan(sqrt(x)) + 165 * pow(z, 6) + pow(pow(x, 2) - z, 0.25);

        printf("y = %.2lf\n", y);

    return 0;

}
